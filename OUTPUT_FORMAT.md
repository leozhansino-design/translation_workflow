# 大纲生成器 - 输出格式说明

## 📁 输出文件结构

生成完成后，会在`outlines/`目录下创建一个文件夹，格式：`书名_类型`

例如：`MyNovel_Horror/`

### 文件夹内容

```
MyNovel_Horror/
├── title.txt              # 小说标题
├── blurb.txt              # 简介/Blurb (少于3000字符)
├── tags.txt               # 标签 (逗号分隔)
├── category.txt           # 类型 (Horror, Romance等)
├── chapter 1 Title.txt    # 第1章大纲
├── chapter 2 Title.txt    # 第2章大纲
├── chapter 3 Title.txt    # 第3章大纲
├── ...                    # 更多章节
└── _full_outline.txt      # 完整AI回复备份
```

## 📝 章节文件格式

每个章节文件内容示例：

```
Chapter 1: The Beginning

Summary:
在这一章中，主角发现了一个神秘的古老手稿...
（200-300词的详细剧情概要）

Key Events:
发现手稿, 遇到神秘人, 接受任务

Characters: Marcus Sterling, Sarah Chen, Professor Williams
```

## ✨ 新功能特性

### 1. 灵活的人名处理
- ✅ 优先使用提供的人名（男10个，女10个）
- ✅ **如果需要更多角色，AI可以自由创造新的英文名**
- ✅ 不再受限于预设的人名列表

### 2. 智能剧情扩展
- ✅ 原文只有2章？没问题！
- ✅ 要求生成15章？AI会**自由扩写剧情**
- ✅ 保证剧情合理、连贯、有吸引力
- ✅ 自动添加情节转折、人物关系、次要情节线

### 3. 章节数可调整
- 📊 在UI中设置"要求章节数"
- 📊 默认：15章
- 📊 范围：5-200章
- 📊 AI会根据要求调整剧情长度

### 4. 纯文本输出
- 📄 不再使用JSON格式
- 📄 所有文件都是.txt，方便编辑
- 📄 每个文件独立，便于批量上传
- 📄 格式固定，易于自动化处理

## 🔧 使用流程

1. **选择原文文件** → `书名_类型.txt`（如`34骨肉汤_Horror.txt`）
2. **设置参数**：
   - 人名数量：男10 女10（作为参考）
   - **要求章节数：15**（可调整）
   - Max Tokens: 100,000
3. **添加到任务队列** → 点击"➕ 添加到任务队列"
4. **预览Prompt** → 点击"👁️ 预览Prompt"查看完整请求
5. **开始生成** → 点击"▶️ 开始生成"
6. **打开文件夹** → 完成后点击"📂 打开文件夹"

## 📊 Prompt关键说明

新Prompt强调：

### 剧情扩展灵活性
```
【剧情扩展要求】⭐
- **原文内容可能很短（例如只有2章）**
- 你需要根据要求的章节数（例如15章、30章、100章）**自由扩展剧情**
- **保证剧情合理、连贯、有吸引力**
- 增加必要的情节转折、人物关系、次要情节线
```

### 人名创作自由
```
【建议人名列表】（可以使用，也可以自己创造新名字）
男性名字参考: Marcus Sterling, James Cooper...
女性名字参考: Sarah Chen, Emily Rodriguez...

- 优先使用提供的英文名作为主要角色
- **如果需要更多角色，可以自由创造新的英文名字**
- 避免中文和拼音，确保人名一致性
```

## 🎯 批量上传优化

所有文件在一个文件夹内：

```bash
# 方便脚本批量处理
for file in MyNovel_Horror/*.txt; do
    if [[ $file == *"chapter"* ]]; then
        # 处理章节文件
    elif [[ $file == "title.txt" ]]; then
        # 处理标题
    fi
done
```

## 💡 最佳实践

1. **查看完整大纲** → 打开`_full_outline.txt`查看AI原始输出
2. **编辑章节** → 直接编辑`chapter X.txt`调整剧情
3. **修改标题/简介** → 编辑`title.txt`或`blurb.txt`
4. **添加标签** → 在`tags.txt`中添加更多标签

## ⚠️ 注意事项

- **文件夹名自动生成**：根据title和genre自动创建
- **重名处理**：如果文件夹已存在，会自动添加时间戳
- **备份完整输出**：`_full_outline.txt`保存了AI的完整回复
- **章节文件名**：使用章节标题作为文件名，方便识别

